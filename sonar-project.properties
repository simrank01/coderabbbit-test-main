# Find these keys on your SonarCloud project homepage (bottom right)
sonar.projectKey=simrank01_coderabbbit-test-main
sonar.organization=simrank01

# Tell Sonar where the code is (the dot means "this folder")
sonar.sources=.

# Optional: Tell Sonar what to ignore (like test folders or logs)
sonar.exclusions=**/node_modules/**,**/dist/**